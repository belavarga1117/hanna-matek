{
  "name": "memoria-muhely",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4186 --directory dist --bind 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "start": "node server/index.js",
    "dev:server": "node --watch server/index.js"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  },
  "engines": {
    "node": ">=22"
  }
}
