PORT=3000
NODE_ENV=development
APP_ORIGIN=http://localhost:3000
DATABASE_URL=postgresql://user:password@localhost:5432/hanna_memoria
BOOTSTRAP_TOKEN=replace-with-a-long-random-one-time-token
# Set to disable only for a local PostgreSQL instance that does not use TLS.
PGSSLMODE=disable
PGPOOL_MAX=10
